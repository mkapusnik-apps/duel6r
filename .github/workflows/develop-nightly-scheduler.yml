name: Develop - Nightly Scheduler
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  scheduler:
    name: Scheduler
    runs-on: ubuntu-latest
    steps:
      - name: Disable scheduler for this dispatch attempt
        run: gh workflow disable develop-nightly-scheduler.yml --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ secrets.PAT }}

      - name: Dispatch nightly build from the sanity tag
        run: gh workflow run develop-nightly.yml --ref sanity --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ secrets.PAT }}

      - name: Restore scheduler after an interrupted dispatch
        if: failure() || cancelled()
        run: gh workflow enable develop-nightly-scheduler.yml --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ secrets.PAT }}
