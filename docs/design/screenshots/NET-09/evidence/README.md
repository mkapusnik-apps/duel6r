# PR #87 intentional host-end provenance

The unchanged `ss023-final.png.json` describes the corrected canonical [host-ended representative](../../../../screenshots/NET-09/host-ended-1280x900.png), not the prior PR #83 image.

The [authoritative screenshot manifest](../../../../screenshots/README.md#network-arena-orientation--current-coverage) owns acceptance. The [durable packet index](../../NET-05/evidence/README.md) explains provenance and limitations; its `host-end-confirm.png` and JSON sidecar record the actual host confirmation before the guest notice.

The guest captured the last confirmed arena after intentional End session following successful reconnect restoration. No crash or timeout was substituted. Temporary paths inside the sidecar are historical provenance, not current dependencies.
